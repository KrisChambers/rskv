[package]
name = "rskv"
version = "0.1.0"
edition = "2024"

[dependencies]
shuttle = { version = "0.9.1", optional = true }
tokio = { version = "1.52.3", features = ["full"] }

[dev-dependencies]
proptest = "1.11.0"

[features]
shuttle = ["dep:shuttle"]
